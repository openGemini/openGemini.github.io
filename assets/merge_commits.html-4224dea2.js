import{_ as e,X as t,Y as a,a3 as s}from"./framework-1e2d737a.js";const n={},i=s(`<h1 id="merge-commits" tabindex="-1"><a class="header-anchor" href="#merge-commits" aria-hidden="true">#</a> Merge commits</h1><p>If you submit a PR, modify it according to the review comments and submit the PR again, and you don&#39;t want the reviewers to see the PR submitted multiple times, because it is not convenient to continue to modify in the review, then you can merge the submitted PR. The PR of the merge submission is realized by compressing the Commit.</p><ol><li>Now view the log on the local branch</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> log
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="2"><li>Then aggregate the top n submission records together and enter. Note that n is a number.</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> rebase <span class="token parameter variable">-i</span> HEAD~n
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Change the pick in front of the logs that need to be compressed to s, s is the abbreviation of squash. Note that one pick must be kept. If all picks are changed to s, there will be no merge target, and an error will occur.</p><ol start="3"><li><p>After the modification is completed, press the ESC key, and then enter <code>:wq</code>, and an interface will pop up, asking you whether to enter the page for editing and submitting notes. After entering e, you will enter the page for merging the commit message. Please delete all the messages that need to be merged, and only keep the message of the merge target, then press the ESC key, and enter <code>:wq</code> to save and exit.</p></li><li><p>Final submission</p></li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">-f</span> origin yourbranch
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="5"><li>Go back to the PR submission page on github, and you can see that the previous submissions have been merged.</li></ol>`,10),o=[i];function r(l,d){return t(),a("div",null,o)}const h=e(n,[["render",r],["__file","merge_commits.html.vue"]]);export{h as default};
